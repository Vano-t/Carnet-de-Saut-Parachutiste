# Copier ce fichier vers .env.local et remplacer par vos vraies valeurs

# OpenWeatherMap API Key
# Obtenez une clé gratuite sur https://openweathermap.org/api
VITE_OPENWEATHER_API_KEY=votre_clé_api_openweathermap

# Supabase Configuration
# Trouvez ces valeurs dans votre dashboard Supabase → Settings → API
VITE_SUPABASE_PROJECT_ID=votre_project_id
VITE_SUPABASE_ANON_KEY=votre_anon_key_public

# Note: Les clés publiques (anon key) peuvent être exposées côté client
# Elles sont protégées par les Row Level Security (RLS) policies de Supabase